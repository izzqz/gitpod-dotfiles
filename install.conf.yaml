- shell:
  - [sudo apt update]
  - [sudo apt install -y neovim httpie]

# See https://github.com/anishathalye/dotbot#configuration
- clean: ['~']

# Shell environment
- defaults:
    link:
        force: true
        relink: true
        mode: 744
                
- link:
    ~/.bashrc: bashrc

# Configurations
- defaults:
    link:
        force: true
        mode: 744
- link:
    ~/.gitconfig:
        path: gitconfig
    ~/.npmrc: npmrc
